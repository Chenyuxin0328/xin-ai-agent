spring:
  profiles:
    active: local
  datasource:
    mysql:
      driver-class-name: com.mysql.cj.jdbc.Driver
      jdbc-url: jdbc:mysql://localhost:3307/xin-ai-agent
      username: root
      password: mysql123
    pgsql:
      driver-class-name: org.postgresql.Driver
      jdbc-url: jdbc:postgresql://localhost/database
      username: chenyuxin
      password: chenyuxin123
server:
  port: 8089
  servlet:
    context-path: /api
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete # ????????????
      logic-delete-value: 1 # ?????????? 1?
      logic-not-delete-value: 0 # ?????????? 0?
# knife4j??????
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    path: /v3/api-docs
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.chenyuxin.xinaiagent.controller
knife4j:
  enable: true
  setting:
    language: zh_cn


